[project]
name = "hello-world"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ande", email = "andesec1@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "openai-agents>=0.4.2",
]

[project.scripts]
hello-world = "hello_world:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = true
